package sept.ex_16092024;

public class Lab032_QnA {
    public static void main(String[] args) {
        // int enum = 10; error >> enum is a keyword >> can't use as a var name
        //System.out.println(enum);
        int Enum = 10; // Valid
        System.out.println(Enum);
    }
}
